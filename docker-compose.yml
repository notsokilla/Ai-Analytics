version: '3.8'

services:
  bot:
    build: .
    container_name: ai-game-bot
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge